# data/processed/

Only synthetic or de-identified sample data goes here — enough to prove
the pipeline in `src/` actually runs, nothing patient-identifiable.

Real processed data lives wherever your `data/raw/` source data lives
(secure server, REDCap, etc.) — state that in the README's Data
Availability section.
